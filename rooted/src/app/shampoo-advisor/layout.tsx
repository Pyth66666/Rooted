import type { ReactNode } from "react";

export const metadata = {
  title: "Find your shampoo | ROOTED Malaysia",
  description: "A free shampoo consultation with real Malaysian listings, ingredient sources and clear reasons for each suggestion.",
};
export default function AdvisorLayout({ children }: { children: ReactNode }) { return children; }
